# AWS Configuration
PUBLIC_AWS_REGION=us-west-2

# =============================================================================
# Cognito Configuration (REQUIRED for authentication)
# =============================================================================
# You must set up AWS Cognito and replace these placeholder values.
# See docs/DEPLOYMENT.md for instructions on creating your Cognito User Pool.
#
# Leave as placeholders to run the app without authentication (gallery only).
# =============================================================================
PUBLIC_COGNITO_USER_POOL_ID=
PUBLIC_COGNITO_USER_POOL_CLIENT_ID=
PUBLIC_COGNITO_IDENTITY_POOL_ID=

# Cognito Hosted UI (for Google OAuth and manual login)
# Format: https://<your-domain>.auth.<region>.amazoncognito.com
PUBLIC_COGNITO_HOSTED_UI_URL=
PUBLIC_COGNITO_HOSTED_UI_DOMAIN=

# =============================================================================
# API Gateway URLs (auto-populated by deploy script)
# =============================================================================
PUBLIC_API_GATEWAY_URL=
PUBLIC_GALLERY_API_URL=

# =============================================================================
# RAGStack Integration (optional)
# =============================================================================
# CloudFront URL for the chat widget script
PUBLIC_RAGSTACK_CHAT_URL=
# GraphQL API for search and upload
PUBLIC_RAGSTACK_GRAPHQL_URL=
PUBLIC_RAGSTACK_API_KEY=
